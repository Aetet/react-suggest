/**
* @jsx React.DOM
*/
var React = require('react');
var Suggest = React.createClass({
  render: function () {
      return (
        <div>
          Suggest
        </div>
      );
  }
});
module.exports = Suggest;